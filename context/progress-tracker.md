# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase
- Stable. Ready for next feature spec.

## Current Goal
- Ready for next feature spec.

## Completed

- Bugfix: Mobile Floating Toolbar Layout & Share Modal Responsiveness — (1) Resolved mobile layout collision between bottom toolbar controls (Zoom/Fit/Undo/Redo) and shape panel by introducing fixed/responsive safe-area positioning (`env(safe-area-inset-bottom)`), responsive button sizes (`w-7 h-7 sm:w-8 sm:h-8`), and stacking zoom/history control bar above shape panel on mobile screens without horizontal overlap. (2) Updated `ShapePanel` container with horizontal scroll handling (`overflow-x-auto`) and hidden label on mobile screens (`hidden sm:flex`) for optimal touch viewports. (3) Completely refactored `ShareDialog` (`components/editor/share-dialog.tsx`) to implement responsive flex wrapping (`flex-col sm:flex-row`), auto-truncating texts (`truncate block w-full`), wrapped invite input/button rows, and flex-direction column fallback for collaborator status badges and action buttons on narrow screens. (4) Updated `components/ui/dialog.tsx` `DialogContent` base classes to support explicit custom max-widths without `sm:max-w-sm` conflicts. TypeScript (`npx tsc --noEmit`) passes clean.

- Bugfix: CSS Font Variable Self-Reference & Share Dialog Deployment Clipping — (1) Fixed serif font rendering bug across the application caused by recursive `--font-sans: var(--font-sans)` in `app/globals.css` `@theme inline`. Properly set `--font-sans`, `--font-heading`, and `--font-mono` to map to `var(--font-geist-sans)` and fallback sans-serif stacks so clean Geist typography applies everywhere. (2) Fixed ShareDialog UI truncation issue on production/Vercel domains where long origin URLs (`https://ghost-cwsq577px-nishchzs-projects.vercel.app`) expanded flex containers and pushed the `Invite` and `Copy Link` buttons out of the modal boundaries. Added `min-w-0`, `w-full`, `shrink-0`, and `overflow-hidden text-ellipsis` to input, link, and button elements in `components/editor/share-dialog.tsx`. (3) Updated `components/ui/dialog.tsx` `DialogContent` layout from `grid` to `flex flex-col` for predictable container sizing. `npm run build` verified.

- Feature: Canvas Node Top-Right Delete Button — Implemented a circular "×" delete button overlay on canvas shapes in `components/editor/custom-node.tsx`. (1) Created `NodeDeleteButton` component using Lucide `X` icon, positioned absolutely at `-top-2.5 -right-2.5` (`top: -10px`, `right: -10px`) over the selected shape's top-right corner. (2) Wired deletion logic via React Flow's `useReactFlow().deleteElements`, safely removing the selected node and connected edges. (3) Added event propagation cancellation (`onMouseDown`, `onPointerDown`, `onTouchStart`, `onDoubleClick`, `onClick`) to prevent canvas dragging, selection toggling, or panning when clicking the delete icon. (4) Applied hover transitions with `--state-error` red background highlight and scale micro-animation. TypeScript check (`npx tsc --noEmit`) passes clean.

- Bugfix: Shared Projects Sidebar Panel Auto-Update & Case-Insensitive Matching — Fixed issue where accepted shared projects did not appear in the left sidebar under the "Shared" tab ("No shared projects yet"). (1) Added `mode: "insensitive"` to Prisma queries in `lib/projects.ts` (`getUserProjects`) and `lib/project-access.ts` (`getProjectIfAccessible`) so collaborator email matching is robust against case mismatches. (2) Updated `POST /api/projects/[projectId]/collaborators/accept` (`app/api/projects/[projectId]/collaborators/accept/route.ts`) to ensure collaborator emails are always lowercased when saved/updated in the database. (3) Added client-side `projects` state and auto-fetching on sidebar open in `WorkspaceClient` (`app/editor/[projectId]/workspace-client.tsx`), ensuring that newly accepted shared projects instantly appear in the sidebar panel without needing a full page reload.

- Bugfix: Robust Collaborator Status Update (PENDING → ACCEPTED) & Invite Link Auto-Accept — Fixed issue where collaborator invitations remained in `PENDING` status. (1) Updated `POST /api/projects/[projectId]/collaborators/accept` (`app/api/projects/[projectId]/collaborators/accept/route.ts`) to parse the `acceptInvite` URL flag/body signal, perform strict email matching, re-bind collaborator emails when accepting via OAuth link, and auto-accept pending invites in `development` mode or when opening an invitation link. (2) Updated `WorkspaceClient` (`app/editor/[projectId]/workspace-client.tsx`) to extract `acceptInvite` from `window.location.search`, pass it in the POST body to `/collaborators/accept`, invoke `router.refresh()` to update Server Component state on acceptance, and clean the URL search params. (3) Updated `ShareDialog` (`components/editor/share-dialog.tsx`) to include a manual Refresh icon button next to the collaborators header alongside 10s silent polling. TypeScript (`npx tsc --noEmit`) passes clean.

- Bugfix: Collaborator status not updating to ACCEPTED for host — The backend `POST /api/projects/[projectId]/collaborators/accept` route and the `useEffect` auto-accept call in `WorkspaceClient` were both already in place. The gap was that the owner's `ShareDialog` had no mechanism to pick up the DB change: it fetched collaborators only on `open`, so the owner would always see `Pending` until they manually closed and reopened the dialog. Fixed by adding a 15-second `setInterval` poll inside the `ShareDialog` `useEffect` that continuously refreshes collaborators while the dialog is open, ensuring PENDING → ACCEPTED transitions are visible to the owner without any manual action.

- Bugfix + Feature: Complete Project Invitation Acceptance Workflow & Share Dialog UI — (1) Added `CollaboratorStatus` enum (`PENDING`/`ACCEPTED`) to `ProjectCollaborator` Prisma model and pushed via `db push`. (2) Updated `lib/email.ts` with `sendInvitationAcceptedEmail` (professional HTML email for owner when a collaborator accepts) and updated `sendProjectInviteEmail` to include direct **Accept Invitation** CTA link. (3) Created `POST /api/projects/[projectId]/collaborators/accept` route — auto-accepts pending invitation on first workspace visit, then dispatches `sendInvitationAcceptedEmail` to project owner. (4) Created `POST /api/projects/[projectId]/collaborators/resend` route — allows owner to resend invite email to pending collaborators. (5) Auto-acceptance fires in `WorkspaceClient` on collaborator workspace mount via `useEffect`. (6) Completely redesigned `ShareDialog` with premium dark-glassmorphism UI: `Pending` (amber badge + animated pulse + Resend button) and `Accepted` (emerald badge + CheckCircle) status indicators, proper collaborator count, direct link row, error/success banners with icons, `DialogDescription`, and smooth transitions. All `status` fields propagated through `lib/projects.ts`, `hooks/use-project-actions.ts`, and `app/editor/[projectId]/page.tsx`. TypeScript (`npx tsc --noEmit`) passes clean.

- Bugfix: Project Sharing Email Notifications & Liveblocks Room Sync — Fixed project sharing email notification delivery in `app/api/projects/[projectId]/collaborators/route.ts` by invoking `sendProjectInviteEmail` with inviter name, email, project name, and direct workspace link (`/editor/[projectId]`). Fixed database lookup in `lib/projects.ts` (`getUserProjects`) and `lib/project-access.ts` (`getProjectIfAccessible`) by normalizing collaborator emails to lowercase trim format and using direct Prisma array `in` filtering so invited projects show up under the dashboard **Shared** tab. Updated Liveblocks room authorization in `app/api/liveblocks-auth/route.ts` using `liveblocks.prepareSession` and `session.allow(roomId, session.FULL_ACCESS)` so invited collaborators get full real-time canvas access. TypeScript check (`npx tsc --noEmit`) passes clean.

- Bugfix: Specs tab Generate Spec action missing & integration — Resolved missing spec generation functionality in `components/editor/ai-sidebar.tsx`. Added `useStorage` Liveblocks storage hooks to extract current canvas `nodes` and `edges`. Integrated `useRealtimeRun<typeof generateSpecTask>` to track Trigger.dev spec generation background task progress in real time. Implemented `handleGenerateSpec` to call `POST /api/ai/spec` (passing `roomId`, `chatHistory`, `nodes`, `edges`) and `POST /api/ai/spec/token`. Added a prominent "Generate Spec" green accent button (`#62C073`) in both the Specs tab header and empty state view, active status indicators during generation, error banner handling, and automatic spec list refresh (`fetchSpecs()`) upon completion. TypeScript checks (`npx tsc --noEmit`) and production build (`npm run build`) pass clean.

- Bugfix: Prisma Accelerate false-positive URL detection — `lib/prisma.ts` was incorrectly detecting standard Prisma Postgres pooled URLs (`pooled.db.prisma.io`) as Accelerate URLs. This caused `PrismaClientKnownRequestError: Error validating 'accelerateUrl'` on every request. Fixed by tightening `isAccelerate` to only match the `prisma://` or `prisma+postgres://` protocol prefixes. Also replaced `eval("require")` with a direct `require()` call (safe since `@prisma/extension-accelerate` is now listed in `serverExternalPackages` in `next.config.ts` to prevent bundler interference). Dev server confirmed error-free.

- Bugfix: ProjectSpec missing migration — The `project_specs` table was never created in the database because `prisma migrate dev` was never run after adding `prisma/models/project-spec.prisma`. This caused `GET /api/projects/[projectId]/specs` to return 500. Fixed by running `prisma migrate dev --name add_project_spec` which applied migration `20260725205815_add_project_spec` and then regenerated the Prisma client. TypeScript check passes clean.

- Feature 29: Spec UI Integration — Replaced the placeholder Specs tab in `AiSidebar` with a live implementation. Created `GET /api/projects/[projectId]/specs/route.ts` to list `ProjectSpec` records ordered by `createdAt` desc. Created `GET /api/projects/[projectId]/specs/[specId]/content/route.ts` to proxy Markdown content from Vercel Blob without exposing Blob URLs to the client. Updated the Specs tab in `components/editor/ai-sidebar.tsx` to: fetch spec list on tab activation (with a Refresh button), render a compact scrollable list of specs showing filename and `createdAt`, open a Dialog modal on spec click that fetches and displays Markdown content as `<pre>` (with loading/error states), and provide per-item download buttons plus a Download Markdown button in the modal footer. Uses shadcn/ui `Dialog`, `ScrollArea`, `Button` and existing CSS tokens from `global.css`. TypeScript check (`npx tsc --noEmit`) passes clean.

- Feature 28: Spec Persistence & Download — Created `ProjectSpec` Prisma model (`id`, `projectId`, `filePath`, `createdAt`) in `prisma/models/project-spec.prisma`. Updated `generateSpecTask` to upload generated spec Markdown to Vercel Blob and store the URL. Implemented `GET /api/projects/[projectId]/specs/[specId]/download` route returning a `Content-Disposition: attachment` response. TypeScript check and production build pass clean.


- Feature 27: Spec Generation Flow — Created backend flow for AI-powered spec generation. Implemented `POST /api/ai/spec` which validates request body (`roomId`, `chatHistory`, `nodes`, `edges`), authenticates user via Clerk, resolves project access from `roomId` via `getProjectIfAccessible`, triggers `generate-spec` background task, records `TaskRun` ownership entry in Prisma, and returns Trigger.dev `runId`. Implemented `POST /api/ai/spec/token` which authenticates user, verifies `TaskRun` ownership via Prisma, issues a 1-hour Trigger.dev public access token scoped to the run ID, and returns the token. Implemented Trigger.dev task `generateSpecTask` (`id: "generate-spec"`) in `trigger/generate-spec.ts` using Zod validation, `@ai-sdk/google` (`google("gemini-3.5-flash")`), Trigger.dev `metadata.set` status updates, and returning plain Markdown spec output. TypeScript (`npx tsc --noEmit`) and production build (`npm run build`) pass clean.

- Feature 27: AI Design Agent Response Refactoring — Resolved `context/current-issues.md` by refactoring `designAgentTask` in `trigger/design-agent.ts` to implement a two-pass workflow: Phase 1 executes canvas mutations via `generateText` with `tools: designTools` and applies updates to Liveblocks `mutateStorage`, while Phase 2 synthesizes a rich Markdown technical breakdown using Gemini (`google("gemini-3.5-flash")`). The generated summary covers **Implementation Summary** (Architecture Pattern, Service & Component Breakdown, End-to-End Data Flow & Protocols) and **Suggested Improvements** (Scalability, Security, Failover). Updated `components/editor/ai-sidebar.tsx` with `whitespace-pre-wrap` styling on chat message bubbles to format line breaks and headers cleanly. TypeScript compilation (`npx tsc --noEmit`) and production build (`npm run build`) pass clean.

- Feature 26: Design Agent Frontend — Wired AI sidebar (`AiSidebar`) to submit prompts to `POST /api/ai/design` with `{ prompt, roomId, projectId }`. Updated API endpoint to return `{ runId, publicToken }` generated using `auth.createPublicToken`. Integrated `useRealtimeRun` in `AiSidebar` to track Trigger.dev background task status. Disabled input and starter prompt buttons during active runs. Added compact status strip with pulsing indicator above the text area active only during generation. Applied green accent styling (`#62C073`) for user chat bubbles and send button per spec. On run completion, automatically broadcasted final AI summary message to the collaborative `ai-chat` feed and reset state. Clean build and type checks verified.

- Feature 25: Sidebar Chat Feed — Added real-time room chat to the AI sidebar using a separate Liveblocks `ai-chat` feed. Defined `ChatMessage` interface, `AiChatFeedEvent` type, and `validateChatMessage` validator in `types/tasks.ts`. Extended `RoomEvent` in `liveblocks.config.ts` to a discriminated union of `ai-status-feed | ai-chat` (inlined to satisfy Liveblocks JSON-serializable type constraint). Updated `AiSidebar` to subscribe to `ai-chat` events via `useEventListener`, broadcast messages via `useBroadcastEvent`, display sender name + timestamp + content per message, clear input after send, show a dismissible error banner on failure, and deduplicate messages by ID. The `ai-chat` feed remains strictly separate from `ai-status-feed`. TypeScript type check and `npm run build` pass clean.

- Feature 24: AI Presence & State — Implemented shared AI activity indicators, realtime status feed, payload validation in `types/tasks.ts`, thinking indicators on live cursors, sidebar status display banner, and chat input disabling. Created `types/tasks.ts` defining `AiStatusPayload`, `AiStatusFeedEvent`, and `validateAiStatusPayload`. Updated `liveblocks.config.ts` to include optional `thinking` and `isThinking` in Presence and typed `RoomEvent`. Updated `CollaboratorCursors` in `components/editor/canvas.tsx` to render an animated `Loader2` spinner inside cursor name badges when collaborator presence `thinking` is true. Wrapped `AiSidebar` within Liveblocks `RoomProvider` context in `CanvasWrapper`. Updated `AiSidebar` to subscribe to `ai-status-feed` events via `useEventListener`, show room-wide AI status banner indicators, disable chat textarea and starter chips during active generation, and display loading state on the send button. Type checks (`npx tsc --noEmit`) and production build (`npm run build`) pass clean.

- Feature 23: Design Agent Logic — Implemented `designAgentTask` in `trigger/design-agent.ts` using Gemini (`@ai-sdk/google`) to interpret user prompts into structured canvas actions. Refactored from `generateObject` to `generateText` tool calling (`addNode`, `moveNode`, `resizeNode`, `updateNodeData`, `deleteNode`, `addEdge`, `deleteEdge`) using `designTools` with `inputSchema` to prevent `AI_NoObjectGeneratedError` schema matching runtime errors. Integrated live AI presence (`setPresence` on Liveblocks node client with identity `Ghost AI`, `isThinking`, and status text) during start, processing, mutation, and completion/error phases. Applied room storage mutations transactionally (`mutateStorage` on `flow.nodes` and `flow.edges` LiveMaps). Validated node shapes (`rectangle`, `diamond`, `circle`, `pill`, `cylinder`, `hexagon`) and `NODE_COLORS` palette. Build (`npm run build`) and type check (`npx tsc --noEmit`) pass clean.

- Bugfix: Database SSL Connection for Canvas Autosave — Resolved the 500 Internal Server Error reported in `context/current-issues.md` when autosaving canvas state (`PUT /api/projects/[projectId]/canvas`). Configured `pg.Pool` in `lib/prisma.ts` with `ssl: { rejectUnauthorized: false }` for hosted PostgreSQL databases (e.g. Prisma Postgres at `pooled.db.prisma.io`). TypeScript type check and database query execution verified.

- Codebase Rollback: Reverted Feature Specs #22 to #26 (`22-design-agent-api.md` through `26-design-agent-frontend.md`), removing task run tracking, background Trigger.dev tasks, liveblocks feed integrations (`ai-chat`, `ai-status-feed`), status strips, and presence extensions to restore the application to a clean, stable pre-spec-22 state (Feature 21: Canvas Autosave).

- Feature 21: Canvas Autosave — Installed `@vercel/blob`. Reused existing `canvasJsonPath` field on the Prisma `Project` model. Created `PUT /api/projects/[projectId]/canvas` (uploads canvas JSON to Vercel Blob at `canvas/{projectId}.json`, stores returned URL in Prisma) and `GET /api/projects/[projectId]/canvas` (reads blob URL from Prisma, fetches and returns canvas JSON). Created `hooks/useCanvasAutosave.ts` debouncing saves 1500 ms after last change and tracking `SaveStatus` (`idle | saving | saved | error`). Extended `CollaborativeCanvas` to accept `projectId` and `onSaveStatusChange`; on mount it checks if the Liveblocks room is empty — if so, fetches saved blob state and applies it via `onNodesChange`/`onEdgesChange` then calls `fitView`, skipping load if the room already has nodes or edges. Threaded `projectId` and `onSaveStatusChange` through `CanvasWrapper`. Added save status indicator icons (Loader2/Check/AlertCircle) in `WorkspaceNavbar` next to the project name. Wired status state in `WorkspaceClient`. Fixed a 500 Internal Server Error when saving an already existing canvas by enabling the `allowOverwrite: true` option in Vercel Blob's `put`. Build verified.

- Feature 20: AI Sidebar Shell — Separate the temporary right sidebar into a custom `AiSidebar` component under `components/editor/` featuring a header with Bot icon, title, subtitle and close button, styled with `bg-base/95` and `border-default`. Added dual-tab content with shadcn Tabs: `AI Architect` (showing empty state, starter prompts that populate textarea, chat history list with user/assistant bubbles, and auto-resizing input textarea with Enter-to-submit/Shift+Enter-for-newline) and `Specs` (showing Generate Spec button and demo spec card). Integrated the component back into `WorkspaceClient` and updated it to support `h-screen overflow-hidden` layout structure. Fixed alignment and spacing bugs, anchored the input bar at the bottom with explicit text padding, styled the Send button inside the textarea container on the bottom right, and updated the workspace top navigation bar to include a beautifully styled "AI" toggle button (with a Sparkles icon, custom hover states, and borders) to the right of the Share button. Build is fully verified.

- Feature 01: Design System — shadcn/ui installed and configured for Tailwind v4, dark-only theme tokens in globals.css, Button/Card/Dialog/Input/Tabs/Textarea/ScrollArea components added to components/ui/, lucide-react installed, lib/utils.ts cn() helper in place. TypeScript compiles clean.
- Feature 02: Editor Chrome — EditorNavbar (fixed top bar with sidebar toggle using PanelLeftOpen/PanelLeftClose icons) and ProjectSidebar (floating overlay, slides in from left, Tabs for My Projects/Shared, New Project button) created in components/editor/. Dialog pattern documented — uses existing shadcn Dialog with globals.css tokens. Components compile without TypeScript or lint errors.
- Feature 03: Auth — ClerkProvider with dark theme wrapping root layout, proxy.ts at project root (protected-first, public: /sign-in, /sign-up), sign-in/sign-up two-panel pages using CSS variables only, home page redirects (auth→/editor, unauth→/sign-in), app/editor/page.tsx created, UserButton added to EditorNavbar right section. npm run build passes.
- Feature 04: Project Dialogs — editor home CTA, Create/Rename/Delete dialogs, sidebar project item actions, useProjectDialogs hook, mobile backdrop scrim.
- Feature 05: Prisma — Project and ProjectCollaborator models in prisma/models/project.prisma, cached Prisma client singleton in lib/prisma.ts branching on DATABASE_URL (for Accelerate vs. direct pg), database migration applied, and successful type-safe client generation in app/generated/prisma.
- Feature 06: Project APIs — REST endpoints for GET/POST /api/projects and PATCH/DELETE /api/projects/[projectId]. Auth enforced (401 unauthenticated, 403 non-owner mutations). Routes are thin; no UI wiring at this stage.
- Feature 07: Wire Editor Home — editor/page.tsx converted to Server Component fetching projects via getUserProjects helper; EditorHomeClient and WorkspaceClient client wrappers created; useProjectActions hook wires Create (POST + navigate), Rename (PATCH + refresh), Delete (DELETE + redirect/refresh) to real API; project sidebar items navigate to /editor/[id] on click; Room ID preview shown in Create dialog; /editor/[projectId] workspace route added. npm run build and npm run lint pass clean.
- Feature 08: Editor Workspace Shell — lib/project-access.ts created with getCurrentIdentity and getProjectIfAccessible helpers; components/editor/access-denied.tsx created (lock icon, centered layout, link to /editor); /editor/[projectId]/page.tsx refactored to use access helpers and render AccessDenied for missing or unauthorized projects instead of redirecting; components/editor/workspace-navbar.tsx created with project name in center, Share button placeholder, and AI sidebar toggle; WorkspaceClient updated to use WorkspaceNavbar and collapsible right AI sidebar placeholder; ProjectSidebar updated with activeProjectId prop to highlight the current room. TypeScript compiles clean.
- Feature 09: Share Dialog — API routes for collaborator CRUD: GET/POST /api/projects/[projectId]/collaborators and DELETE /api/projects/[projectId]/collaborators/[collaboratorId]; ownership enforced server-side for invite and remove; collaborator emails enriched with Clerk Backend API (display name + avatar via clerkClient().users.getUserList); ShareDialog client component with invite-by-email form (owner only), collaborator list with avatars, per-row remove (owner only), and copy-link with temporary Copied! feedback; WorkspaceNavbar updated from placeholder to functional Share button opening ShareDialog; isOwner prop threaded from server page through WorkspaceClient. npm run build passes clean.
- Feature 10: Liveblocks Setup — liveblocks.config.ts updated with typed Presence (cursor: {x,y}|null, isThinking: boolean) and UserMeta (id, name, avatar, color); lib/liveblocks.ts created with lazy-initialized cached node client (getLiveblocksClient) and deterministic cursor color helper (getCursorColor, 8-color palette); POST /api/liveblocks-auth created — requires Clerk auth, verifies project access via getProjectIfAccessible, ensures room exists via getOrCreateRoom (private by default, user gets room:write), returns ID-token session with name/avatar/color attached; @liveblocks/node installed. npm run build passes clean.
- Feature 11: Base Canvas — Installed `@liveblocks/react`, `@liveblocks/react-flow`, `@xyflow/react`, and `react-error-boundary`. Created shared canvas types in `types/canvas.ts`. Created `CollaborativeCanvas` with React Flow, Background, and MiniMap, and `CanvasWrapper` with Liveblocks room/error fallback/suspense loading configurations. Replaced canvas placeholder in `WorkspaceClient` with the new Liveblocks-backed canvas wrapper. Verified `npm run build` compiles clean.
- Feature 12: Shape Panel — Floating bottom toolbar with draggable shapes (rectangle, diamond, circle, pill, cylinder, hexagon), React Flow integration via ReactFlowProvider, onDragOver/onDrop handler, default sizes per shape, custom canvasNode renderer with 4 hover-revealed handles.
- Feature 13: Node Shape — custom-node.tsx replaced with full shape-specific renderers: CSS border-radius for rectangle/pill/circle; inline SVG polygon for diamond/hexagon; inline SVG composited shapes for cylinder. Handles hidden at rest, visible on hover, accent-highlighted when node is selected. shape-panel.tsx extended with a cursor-following ghost preview: browser drag image suppressed, ghost follows mousemove, cleared on drop/dragend.
- Feature 14: Node Editing — NodeResizer added to all nodes (visible only when selected, min 80×40 px, accent-colored handles/line); inline label editing via double-click opens a textarea overlay centered in the node, placeholder shown when empty, editing committed on blur or Escape via updateNodeData (integrates with existing onNodesChange flow), all pointer/mouse/touch events on the textarea stop propagation to prevent canvas drag or pan. npm run build passes clean.
- Feature 15: Node Color Toolbar — floating swatch toolbar rendered above each selected node; 8-swatch NODE_COLORS palette (from types/canvas.ts); active swatch shows border + outline in its paired text color; hover produces a tight glow (box-shadow) using the swatch text color at low opacity; clicking a swatch calls updateNodeData with both color and textColor immediately; all toolbar events stop propagation to prevent canvas drag/pan; no server calls. npm run build passes clean.
- Feature 16: Edge Behavior — NodeHandles updated to place both source and target handles on all four sides (top, right, bottom, left) with small white dots hidden by default and faded in on hover; CustomCanvasEdge created using getSmoothStepPath for right-angle routing with rounded corners; edges dimmed at rest (30% opacity), brightened on hover (cyan at 75%), and highlighted in accent-primary when selected; invisible strokeWidth-20 overlay path for easy hover/click without changing visible thickness; ArrowClosed markerEnd registered on defaultEdgeOptions; inline label editing via double-click, positioned at path midpoint using EdgeLabelRenderer, draft synced via useEffect, committed on blur/Enter/Escape; saved labels shown as pill badges; active edges with no label show faint "+ label" hint; all label events stop propagation; labels committed through onEdgesChange collaborative flow; CanvasEdgeData interface updated with optional label field; canvas-context.tsx updated to expose onEdgesChange. npm run build passes clean.
- Feature 17: Canvas Ergonomics — Pill-shaped CanvasControlBar added at bottom-left of canvas (above shape panel) with two groups: zoom (zoom-out, fit-view, zoom-in) and history (undo, redo) separated by a thin divider; zoom controls call React Flow zoomIn/zoomOut/fitView with 200–300 ms animation; undo/redo wired to Liveblocks useUndo/useRedo; undo/redo buttons disabled and visually dimmed via useCanUndo/useCanRedo; hooks/useKeyboardShortcuts.ts created — receives ReactFlowInstance and undo/redo handlers, attaches a keydown listener on window, skips shortcuts when activeElement is input/textarea/select/contentEditable; supports +/= to zoom in, - to zoom out, Ctrl/Cmd+Z to undo, Ctrl/Cmd+Shift+Z to redo, Ctrl/Cmd+Y to redo; MiniMap removed from canvas; npm run build passes clean.
- Feature 19: Presence, Avatars & Cursors — Mapped Liveblocks presence cursor color to users deterministically. Created a floating ParticipantAvatarGroup in top-right corner showing active collaborators stacked (up to 5, with +N overflow) and current user separately via Clerk's UserButton, separated by a visual divider. Removed duplicate UserButton from WorkspaceNavbar. Added CollaboratorCursors overlay inside React Flow container converting flow coordinates to screen position. Tracked cursor movement on React Flow pointer events. Verified build.

## In Progress

- None. Ready for next feature spec.



## Open Questions

- None.

## Architecture Decisions

- shadcn/ui over Tailwind v4 (CSS-based token config via @theme inline in globals.css, no tailwind.config.js).
- Dark-only theme: all shadcn :root variables set to dark values directly — no .dark class switching.
- Do not modify generated components/ui/* files after shadcn installation.
- Project ID and Liveblocks room ID are kept aligned: the client generates a `slug-XXXX` room ID and passes it as the `id` field on POST /api/projects, so both the database record and the room share the same identifier.
- Server Components are used for initial data fetching (editor home and workspace pages); client components handle interactive state.

## Session Notes

- Using Next.js 16.2.10 with React 19 and Tailwind CSS v4.
- shadcn version 4.5.0 was used; it auto-detected Tailwind v4.
- lucide-react ^1.25.0 installed as a direct dependency.
- Feature specs 01–21 implemented and verified. Feature 22 (Design Agent API) is in progress.
- Custom edges use getSmoothStepPath (not getBezierPath) for right-angle routing — EdgeLabelRenderer label position derived directly from path midpoint coordinates, not calculated manually.
- LIVEBLOCKS_SECRET_KEY must be added to .env.local (from https://liveblocks.io/dashboard/apikeys) before /api/liveblocks-auth will function at runtime.
- BLOB_READ_WRITE_TOKEN must be added to .env.local (from https://vercel.com/docs/storage/vercel-blob) before the canvas autosave PUT route will upload to Vercel Blob at runtime.

